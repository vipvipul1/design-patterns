package strategy.example.maps.problem.v1;

public class Map {
    public String getRoute(String src, String dest) {
        // Algorithm for finding the shortest distance from src to dest.
        return "Shortest distance from " + src + " to " + dest + " via Car route";
    }
}
